@page
@model AILibrary.Pages.Admin.Library.IndexModel
@{
    ViewData["Title"] = "Gerenciar Biblioteca";
}

<div class="admin-section-header">
    <div>
        <h1 class="page-title" style="font-size: 32px; margin-bottom: 0.25rem;">Gerenciar Biblioteca</h1>
        <p style="color: var(--text-secondary); font-size: 13px;">Adicione, edite ou remova recursos da biblioteca digital.</p>
    </div>
    <a asp-page="./Edit" class="admin-btn admin-btn-primary">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" style="width: 14px; height: 14px;">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 4v16m8-8H4" />
        </svg>
        Novo Recurso
    </a>
</div>

@if (!string.IsNullOrEmpty(Model.SuccessMessage))
{
    <div class="admin-alert admin-alert-success">
        ✓ @Model.SuccessMessage
    </div>
}

<div style="margin-bottom: 1.5rem;">
    <form method="get" style="display: flex; gap: 0.75rem;">
        <input type="text" name="SearchQuery" value="@Model.SearchQuery" class="form-control" placeholder="Buscar por título, código ou slug..." style="max-width: 400px;" autocomplete="off" />
        <button type="submit" class="admin-btn admin-btn-secondary">Buscar</button>
        @if (!string.IsNullOrEmpty(Model.SearchQuery))
        {
            <a asp-page="./Index" class="admin-btn admin-btn-secondary" style="border: none;">Limpar</a>
        }
    </form>
</div>

<div class="admin-table-container">
    <table class="admin-table">
        <thead>
            <tr>
                <th style="width: 60px;">ID</th>
                <th>Recurso</th>
                <th>Item Code</th>
                <th>Tipo</th>
                <th>Categoria</th>
                <th>Novo?</th>
                <th style="width: 180px; text-align: right;">Ações</th>
            </tr>
        </thead>
        <tbody>
            @if (!Model.Items.Any())
            {
                <tr>
                    <td colspan="7" style="text-align: center; padding: 3rem; color: var(--text-muted);">
                        Nenhum recurso encontrado.
                    </td>
                </tr>
            }
            else
            {
                foreach (var item in Model.Items)
                {
                    <tr>
                        <td style="font-family: var(--font-mono); font-weight: 700; color: var(--accent);">#@item.Id</td>
                        <td>
                            <div style="font-weight: 600; color: var(--text-primary);">@item.Title</div>
                            <div style="font-size: 11px; color: var(--text-muted); font-family: var(--font-mono);">@item.Slug</div>
                        </td>
                        <td style="font-family: var(--font-mono); font-size: 12px;">@item.ItemCode</td>
                        <td>
                            <span class="detail-type-badge" style="font-size: 9px; padding: 0.15rem 0.4rem; vertical-align: middle;">
                                @item.Type.ToUpper()
                            </span>
                        </td>
                        <td>
                            <span class="detail-category-badge" style="font-size: 9px; padding: 0.15rem 0.4rem; vertical-align: middle;">
                                @item.Category.ToUpper()
                            </span>
                        </td>
                        <td>
                            @if (item.IsNew)
                            {
                                <span style="color: var(--badge-new); font-weight: 700; font-size: 11px;">SIM</span>
                            }
                            else
                            {
                                <span style="color: var(--text-muted); font-size: 11px;">NÃO</span>
                            }
                        </td>
                        <td>
                            <div class="admin-actions-cell" style="justify-content: flex-end;">
                                <a asp-page="./Edit" asp-route-id="@item.Id" class="admin-btn admin-btn-secondary admin-btn-sm">
                                    Editar
                                </a>
                                <a asp-page="./Delete" asp-route-id="@item.Id" class="admin-btn admin-btn-danger admin-btn-sm">
                                    Excluir
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>

@if (Model.TotalPages > 1)
{
    <div style="display: flex; justify-content: space-between; align-items: center; margin-top: 1rem; margin-bottom: 2rem;">
        <span style="color: var(--text-secondary); font-size: 13px;">
            Página <strong>@Model.PageNum</strong> de @Model.TotalPages
        </span>
        <div style="display: flex; gap: 0.5rem;">
            @if (Model.HasPreviousPage)
            {
                <a asp-page="./Index" asp-route-PageNum="@(Model.PageNum - 1)" asp-route-SearchQuery="@Model.SearchQuery" class="admin-btn admin-btn-secondary admin-btn-sm">
                    ← Anterior
                </a>
            }
            else
            {
                <button class="admin-btn admin-btn-secondary admin-btn-sm" disabled style="opacity: 0.4; cursor: not-allowed;">
                    ← Anterior
                </button>
            }

            @if (Model.HasNextPage)
            {
                <a asp-page="./Index" asp-route-PageNum="@(Model.PageNum + 1)" asp-route-SearchQuery="@Model.SearchQuery" class="admin-btn admin-btn-secondary admin-btn-sm">
                    Próxima →
                </a>
            }
            else
            {
                <button class="admin-btn admin-btn-secondary admin-btn-sm" disabled style="opacity: 0.4; cursor: not-allowed;">
                    Próxima →
                </button>
            }
        </div>
    </div>
}

<div style="margin-top: 1.5rem;">
    <a asp-page="/Admin/Index" class="admin-btn admin-btn-secondary">
        ← Voltar ao Painel
    </a>
</div>
